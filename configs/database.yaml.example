# PostgreSQL（复制为 configs/database.yaml 并填写）
database:
  url: "postgresql+psycopg://icu_dev:CHANGE_ME@localhost:5432/icu_scheduling"
  pool_size: 5

# Layer 0 只读源（MIMIC 导入完成后填写）
layer0:
  mimic_source: demo   # demo | full | mimic
  demo_dsn: "postgresql+psycopg://icu_dev:CHANGE_ME@localhost:5432/mimic_iv_demo"
  full_dsn: "postgresql+psycopg://icu_dev:CHANGE_ME@localhost:5432/mimic_iv"
  mimic_dsn: "postgresql+psycopg://icu_dev:CHANGE_ME@localhost:5432/mimic"
  waveform_dsn: "postgresql+psycopg://icu_dev:CHANGE_ME@localhost:5432/mimic_iv_waveform"
