# 复制为 configs/data.yaml
source: mimic   # mock | mimic — use mimic after restoring a Layer1 dump
phase: P0      # P0 | P1 | P2

mock:
  schema: mock

mimic:
  # 使用 database.yaml 中 layer0 的 DSN，由 ETL 按 mimic_source 选择
  pass
